name: run-tests

on:
  push:
    branches:
      - main
      - master
    paths:
      - "src/**"

defaults:
  run:
    working-directory: src

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Runs Tests
        run: docker run -f ./Dockerfile.dev yarn test -- --coverage
